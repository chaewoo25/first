# 🤖 Mini NPU Simulator (패턴 매칭 & MAC 연산)

외부 AI/선형대수 라이브러리(NumPy, PyTorch 등)를 일체 사용하지 않고, 순수 파이썬(Pure Python)으로 인공지능 NPU의 핵심 연산인 **MAC (Multiply-Accumulate, 곱셈-누적)** 연산 로직 및 성능 프로파일러를 구현한 프로젝트입니다.

---

## 1. 실행 방법

```bash
# 3주차 폴더로 이동
cd 3주차

# 시뮬레이터 실행
python main.py